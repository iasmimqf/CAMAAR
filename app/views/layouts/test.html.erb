<!DOCTYPE html>
<html>
  <head>
    <title>Camaar Test</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style>
      /* CSS simples inline para testes */
      body { font-family: Arial, sans-serif; margin: 20px; }
      .container { max-width: 800px; margin: 0 auto; }
      .btn { background: #007bff; color: white; padding: 10px 15px; border: none; cursor: pointer; }
      .btn:hover { background: #0056b3; }
      .form-control { width: 100%; padding: 8px; margin: 5px 0; border: 1px solid #ccc; }
      .error { color: red; }
      .success { color: green; }
      .questao-item { border: 1px solid #ddd; padding: 15px; margin: 10px 0; }
    </style>
  </head>

  <body>
    <div class="container">
      <% if notice %>
        <p class="notice success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert error"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
